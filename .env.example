# STREAM CORPORATION OPS DECK CONFIGURATION
PORT=
NODE_ENV=

# Master Admin Key for root access control
MASTER_ADMIN_KEY=

# MongoDB Connection URI (Optional: if omitted, automatic local JSON persistence is used)
# MONGODB_URI=mongodb+srv://<user>:<password>@cluster.mongodb.net/stream_corp?retryWrites=true&w=majority

# Session & Security Secret
SESSION_SECRET=
